%h1 Editing book
%h4 (Currently known as <i>#{@book.title}</i>)


-# Refer to http://guides.rubyonrails.org/active_record_validations_callbacks.html#working-with-validation-errors
-# This next line causes raw output of {}
-# =@book.errors
=render 'book'

%p #{ link_to 'Cancel Edits', @book } | #{ link_to 'Back to Main index', books_path }
