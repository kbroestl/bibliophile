%h1 New author
= @author.errors
- form_for(@author) do |f|
  %p #{f.label :name, "Author Name:"} #{f.text_field :name}
  %p.smaller Include all details here exactly as they should be presented later. 
  %p #{f.submit 'Create'}

%p #{ link_to 'Back', authors_path }